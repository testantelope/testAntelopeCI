workspace.dir					= ../..
thridpart.dir						= ${workspace.dir}/com.antelope.ci.bus.thirdpart
thridpart.bundle.dir			= ${thridpart.dir}/bundle
project.dir							= ..
build.dir							= ${project.dir}/build
bundle.dir							= ${project.dir}/bundle
bin.dir								= ${project.dir}/bin
conf.dir								= ${project.dir}/conf
test.bat								= ${build.dir}/test.bat
test_debug.bat					= ${build.dir}/test_debug.bat
test.sh								= ${build.dir}/test.sh

#logger configuration
logger.src.dir						= ${project.dir}/src/logger
logger.maven					= ${logger.src.dir}/pom.xml
logger.target.dir				= ${logger.src.dir}/target
logger.dependency.dir		= ${logger.target.dir}/dependency
logger.src.conf.dir				= ${logger.src.dir}/conf
logger.test.dir					= ${basedir}/logger-test
logger.test.bundle.dir		= ${logger.test.dir}/bundle
logger.test.bin.dir				= ${logger.test.dir}/bin
logger.test.conf.dir			= ${logger.test.dir}/conf
logger.test.lib.dir				= ${logger.test.dir}/lib

#server configuration
server.src.dir						= ${project.dir}/src/server
server.maven						= ${server.src.dir}/pom.xml
server.target.dir					= ${server.src.dir}/target
server.dependency.dir		= ${server.target.dir}/dependency
server.src.conf.dir				= ${server.src.dir}/conf
server.test.dir					= ${basedir}/server-test
server.test.bundle.dir			= ${server.test.dir}/bundle
server.test.bin.dir				= ${server.test.dir}/bin
server.test.conf.dir			= ${server.test.dir}/conf
server.test.lib.dir				= ${server.test.dir}/lib

